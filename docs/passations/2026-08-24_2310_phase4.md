# Passation — 2026-08-24 23:10 — phase 4

## Fait
- **Phase 4 démarrée** (feu vert utilisateur « continue »). Ordre des écrans figé dans
  `ETAT.md`. Écran 1 = coquille applicative (`web/src/components/layout/AppLayout.vue`).
- **Inventaire + états écrits** dans `ETAT.md` (étapes 1-2 de la skill `migration-ecran`),
  y compris un arbitrage utilisateur explicite sur la restructuration nav (maquette à 4
  destinations vs 6 existantes → 5 pastilles primaires + Communauté en icône secondaire,
  mobile 4 onglets + Planning/Communauté en icônes d'en-tête — aucune destination
  supprimée).
- **Implémentation (étapes 3-6)** faite par un sous-agent `migrateur-ecran` : bascule
  structurelle sidebar→nav en-tête desktop (conforme à la maquette Direction A), indicateur
  hors ligne ajouté (fonctionnalité neuve, absente avant). 41 tests, rouge confirmé → vert.
- **Revue de code** (contrôleur, modèle le plus capable) : conforme sur la règle d'or et
  l'arbitrage nav, vérifié contre le diff. 4 constats Important réels trouvés (bug de boîte
  `pb-safe`, collision avec le FAB Pomodoro existant, 3 contrôles sous 44px malgré le
  rapport, densité mobile 375px tendue).
- **Captures d'écran réelles prises par le contrôleur** (~1080px, navigateur maintenant
  connecté — voir note plus bas sur les limites de cet environnement) : **bug confirmé,
  clair ET sombre** — la boîte de recherche chevauchait visuellement la pastille
  « Réviser », la cloche de notification chevauchait « Classes ».
- **Tour de correction 1 dispatché et terminé** (même sous-agent, un aller-retour réseau
  interrompu au milieu, repris sans perte — voir ledger de conversation) : 8 constats
  consolidés corrigés. Suite complète re-confirmée verte : **196/196**, `vue-tsc -b`
  propre. Détail dans `docs/passations/scratch-appshell-migration-report.md` (section
  « Fix round 1 »).
  - Item 1 (chevauchement) : corrigé par le sous-agent en poussant certains éléments
    (libellé recherche + `⌘K`, pastille date) du seuil `md`/`sm` à `xl` (1280px) — mais
    **non re-vérifié visuellement** par le sous-agent (pas d'outils navigateur) ni par le
    contrôleur (interrompu par l'avertissement de limite d'usage juste avant de le faire).
    C'est estimé arithmétiquement seulement (~9-75px de marge selon le sous-agent).
  - Item 5 (troncature titre mobile 375px) : `truncate`+`min-w-0` ajoutés, badge hors ligne
    réduit à icône seule sous `sm`. Marge encore tendue (~19-29px estimés) si le badge hors
    ligne est affiché — sous-agent a volontairement laissé la décision d'une restructuration
    plus large (en-tête mobile sur deux lignes, ou suppression du titre mobile) à un humain,
    conformément à la consigne de ne pas l'inventer seul.
  - Un bug réel non lié trouvé et corrigé au passage : `PomodoroTimer.vue` avait un
    commentaire HTML avant sa racine, en faisant un fragment multi-racine (cassait
    `wrapper.classes()` en test) — déplacé à l'intérieur.

## État
- **Aucun sous-agent en cours.**
- `main` : working tree **non committé** (volontaire — le cycle n'est pas fini, il manque
  la re-vérification visuelle de l'item 1 + la décision sur l'item 5 + les captures finales
  avant le commit de l'étape 8). Fichiers modifiés :
  `web/src/components/layout/AppLayout.vue`, `web/src/components/ui/PomodoroTimer.vue`,
  `ETAT.md` ; nouveaux : `web/tests/components/layout/AppLayout.spec.ts`,
  `web/tests/components/ui/PomodoroTimer.spec.ts`,
  `docs/passations/scratch-appshell-migration-report.md` (rapport de travail, pas un doc
  permanent). Rien de perdu, tout est sur disque, aucun risque à un `/clear` ou reset.
- **Serveur de dev frontend tourne** : `npm run dev -- --port 3000` lancé depuis `web/`,
  PID en arrière-plan (voir `netstat -ano | grep :3000` si besoin de le retrouver/tuer).
  Reflète l'état actuel non committé (hot-reload Vite).
- **Backend Docker : bloqué, non fonctionnel.** `docker compose up -d db redis backend` a
  démarré `db`/`redis` sains, mais `backend` crash en boucle :
  `password authentication failed for user "studyhub"`. Le volume Postgres existant
  (`studyhub_db`, créé lors d'une session précédente) a un mot de passe différent de celui
  dans `.env` (généré cette session, `POSTGRES_PASSWORD=changeme` après une tentative —
  ni `devlocal` ni `changeme` ne correspondent au mot de passe réel du volume existant).
  **Je n'ai pas essayé de le réinitialiser** (`docker compose down -v` supprimerait le
  volume et toute donnée de test qu'il contient) — décision volontaire de ne pas faire
  d'action destructive sans consentement explicite. Deux fichiers créés pour ce diagnostic,
  tous deux gitignorés donc sans risque de commit accidentel :
  - `.env` (racine du repo) — valeurs de dev local, mot de passe actuellement `changeme`.
  - `docker-compose.override.yml` (racine du repo) — publie le port `5000:5000` du service
    `backend` vers l'hôte (absent du compose de base, qui n'expose que `nginx:80`).
- **Extension Chrome connectée** cette session (après un aller-retour de dépannage
  utilisateur) — mais l'environnement de screenshot a une **contrainte physique
  découverte cette session** : la largeur de viewport CSS maximale atteignable via
  `resize_window` semble plafonnée à environ **1080px** (juste au-dessus du seuil `lg`
  1024px), quel que soit la largeur demandée (testé jusqu'à 2300px sans effet) — l'écran
  physique de la machine ne fait que ~1472px et une portion fixe est absorbée par le chrome
  du navigateur/l'extension. Impossible d'atteindre la largeur standard du projet (1440px)
  ni une largeur mobile fiable (375px) par cette voie — une tentative de contournement par
  `document.documentElement.style.zoom` a gelé le renderer (récupéré en fermant l'onglet).
  À la reprise, retenter directement à ~1080px (fiable) plutôt que de re-explorer le
  contournement zoom.

## Prochaine action (dans l'ordre)
1. **Résoudre le blocage backend** (mot de passe Postgres du volume existant) — soit
   l'utilisateur connaît/retrouve le mot de passe réel, soit consentement explicite pour
   `docker compose down -v` (supprime le volume, table rase) suivi d'un `docker compose up`
   qui réinitialisera avec le mot de passe de `.env`. **Demander avant d'agir.**
2. **Re-vérifier visuellement l'item 1** (chevauchement en-tête) à ~1080px, clair ET
   sombre — le point bloquant restant avant de pouvoir finaliser l'Écran 1. Navigateur déjà
   connecté ; réutiliser le flot `localStorage.setItem('sh_token', ...)` (voir historique de
   conversation pour le script exact) pour contourner l'auth réelle si le backend n'est
   toujours pas debloqué, OU se connecter pour de vrai une fois le backend up.
3. Si l'item 1 est bon : décider de l'item 5 (titre mobile — accepter tel quel, ou
   restructurer) ; sinon, nouveau tour de correction ciblé.
4. Finaliser l'étape 8 : mettre à jour `ETAT.md` (cocher Écran 1), **commit** (le rapport de
   travail `docs/passations/scratch-appshell-migration-report.md` peut être supprimé après,
   ce n'est pas un doc permanent).
5. Enchaîner Écran 2 (Accueil/dashboard fusionné) selon l'ordre déjà fixé dans `ETAT.md`.

## À relire en priorité
- Ce fichier
- `ETAT.md`, section « Écran 1 — Coquille applicative » (sous « Phase courante — 4 »)
- `docs/passations/scratch-appshell-migration-report.md` (rapport détaillé du sous-agent,
  y compris la section « Fix round 1 »)
